interface:
  display_name: "Fabricsg"
  short_description: "Repo-specific patterns and workflows for fabricsg"
  default_prompt: "Use the fabricsg repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true